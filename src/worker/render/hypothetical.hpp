#include <sstream>

#include "../graph.hpp"

namespace render {

  void hypothetical(Graph const &,
                    typename Graph::vertex_descriptor root,
                    std::stringstream & ss);

}
